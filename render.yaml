services:
  # ── Django Backend ─────────────────────────────────────────────────────────
  - type: web
    name: assessment-hub-backend
    runtime: python
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate && python manage.py seed_all_tests && python manage.py setup_admin
    startCommand: python start_server.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.8"
      - key: SECRET_KEY
        generateValue: true   # Render auto-generates a secure random secret key
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: assessment-hub-backend.onrender.com
      - key: CORS_ALLOWED_ORIGINS
        value: https://assessment-hub-frontend.onrender.com
